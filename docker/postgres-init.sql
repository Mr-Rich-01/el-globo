-- PostgreSQL init script - runs only on first DB creation
-- Extensions úteis
CREATE EXTENSION IF NOT EXISTS "pg_trgm"; -- Para pesquisa fuzzy de produtos
CREATE EXTENSION IF NOT EXISTS "unaccent"; -- Para pesquisa sem acentos
